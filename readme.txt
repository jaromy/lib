Description of files

getch - emulates DOS/Windows version of this function. Captures single key presses without also requiring newline character.

MovingAverage_3 - implements a moving (running) average algorithm, using a window size of 3. Implemented for unsigned integers only. 